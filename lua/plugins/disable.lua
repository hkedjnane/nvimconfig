return {
  -- { "nvim-neo-tree/neo-tree.nvim", enabled = false }
  -- { "neovim/nvim-lspconfig", enabled = false }, --require("lazy-nix-helper").mason_enabled() },
  { "williamboman/mason.nvim", enabled = false }, --require("lazy-nix-helper").mason_enabled() },
  { "williamboman/mason-lspconfig.nvim", enabled = false } --require("lazy-nix-helper").mason_enabled() }
}
